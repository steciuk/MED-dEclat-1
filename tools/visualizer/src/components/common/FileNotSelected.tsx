import React from 'react';

const FileNotSelected = () => {
	return (
		<div style={{ height: '100%', width: '100%', display: 'grid', placeItems: 'center', fontSize: '1.5rem' }}>
			File not selected
		</div>
	);
};

export default FileNotSelected;
